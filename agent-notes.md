# agent-notes.md — BrioCare implementation plan & context

Working memory for the agent across compressed conversations. Keep updated as steps complete (check boxes, add discoveries). Human = Milton (milton.x.ren@gmail.com).

## Project snapshot

- **BrioCare**: 48-hour AI Fund build challenge (Engineer-in-Residence). Prototype = **Brio**, a voice AI co-facilitator for clinician-supervised pediatric group teletherapy (telehealth, kids 8–12).
- Deliverables: PRD ✅, TDD ✅, working prototype + exact run command + exact test command, then a 45-min AI interview where Milton must walk the core loop, read code aloud, make a live change, and explain what the model receives/emits/gets wrong. **Code must be interview-narratable.**
- Two technical spikes we're claiming: (1) the trigger→actor→constraints→gate architecture with full decision audit; (2) sim-persona eval harness with equity metrics + safety invariants.
- Submission reply format: PRD / TDD / Prototype / Source code / Run command or live URL / Test command / Access notes / Known limitations.

## Key artifacts & files

- PRD v0.9: `docs/prd.html` → artifact https://claude.ai/code/artifact/281e36ab-f9f8-4b3d-bf62-5e332090279b
  - v0.9 = v0.8 + **7 riso-style illustrations** inlined as WebP data URIs, **no captions** (Milton: captions restated the adjacent prose). Alt text retained. Sources `docs/img/{1..7}.png` — all regenerated at a uniform ~2.4:1 — web copies `{1..7}.webp` (752 KB total; `cwebp -q 82 -resize 1800 0 -m 6`). Prompts + art direction: `docs/image-brief.md` — regenerate from there to keep the style consistent.
  - Figure CSS = `figure.plate` only (paper-ground plate + thin rule, full measure; images get `brightness(.9)` in dark mode so they don't glare). Artifact favicon 🪑.
  - Known nit: image 3 (§02) now reads as two people side by side rather than one person printed in two ink plates — weakens "a two-person job, staffed by one". Regenerate if it bothers a reviewer.
- TDD **v0.8** ("Reflexes, judgment, and a veto"): `docs/tdd.html` → artifact https://claude.ai/code/artifact/d38e028b-f753-449c-9723-84cd74ca3d9e (favicon 🧠 — keep stable). v0.8 = the full doc-truth pass after the live-testing cycle: breakout-room/handoff model, three dials (instant default), §03 rewritten as "One invite, end to end" (debounce→round→commit vetoes→delivery gate→voicedAt→grace-until-response), §04 trigger table rebuilt (handoff gate/round/followup/equity-as-fallback + real numbers), §05 now FOUR gates (menu+hard-veto · prompt · human · the floor), §06 full-timeline+decision-history context + "three ways it goes wrong… the prompt shapes behavior; the gates guarantee it", §07 ERD +ladderLevel/voicedAt/yielded + one-row-per-thought, §10 failure table +4 observed rows and 4 rows trued to what's actually built (no confidence gating/panel notices/auto-rejoin claims), appendix entries 11–13 (room-leader pivot, the delivery gate, prompt-rules→mechanisms). Republish flow gotcha: a cross-session artifact publish requires reading the saved live copy IN FULL + a fresh Artifact read fetch before publish is accepted.
- Field research: `research/pediatric-group-therapy.html`
- **Doc convention**: each doc = hidden artifact body (`docs/.prd.artifact-body.html`) + standalone (`docs/prd.html` = body wrapped in doctype/head; regenerate with the small python wrapper script used throughout). Republish via Artifact tool with SAME file path → same URL. Bump version in mast-meta + footer each edit; label `prd-vX.Y`.
- `human_notes.md` is Milton's — **never edit it** (reading ok).
- Milton's preferences: aggressive simplification, concise answers, discuss-before-change on design, keep repo root minimal (hence `src/`), docs must stay truthful to the build.

## Architecture (locked, v0)

**"Reflexes, judgment, and a veto"** — two runtimes:

1. **Media worker** (`src/worker/`, LiveKit agents-js, a dumb pipe): joins room as participant "Brio"; per-track STT via **LiveKit Inference**; speaking events → airtime; publishes Brio's TTS voice on its own audio track (Brio appears as a real participant tile with speaking indicators); watches `agentIntents` — pre-synthesizes TTS on `pending`, plays on `executed`. No decisions.
2. **The brain** (`src/backend/` Convex): deterministic **triggers** (rules + 10s sweep + therapist taps) wake a single-call LLM **actor** with full context + recommendation + **menu of legal actions**; actor returns structured `{action, target?, utterance?, reason}` or `do_nothing` (utterance written in the SAME call); **hard constraints** re-check output; **therapist gate** (veto window / tap) before anything is voiced.

Core rules:
- Autonomy dial: **autonomous = default** (~4s veto window, auto-execute on lapse; TTS pre-synthesized so playback is instant); suggest-only = one tap away (nothing voiced without tap).
- Cue / named moves (draw_out, block, cut_off, link): therapist-initiated, `do_nothing` OFF the menu, tap = approval, no window. Session greeting = a cue (therapist speaks first, then cues Brio's intro). No special start process.
- Distress: **two detectors** → same `flags` table, deduped by utteranceId: inline watchlist tripwire (zero latency, no LLM) + evaluator sweep every **10s** (menu: raise_flag | do_nothing). Flags bypass the gate (born executed). Never voiced, ever. Private panel alert + ack.
- **Every actor decision logged** in `agentIntents` incl. `do_nothing` (no card/display; audit only) with `prompt` + `llmResponse`.
- Intent lifecycle: `pending → executed | canceled(therapist|muted|stale)`. Transitions are **guarded Convex mutations** (succeed only while pending) — approve/cancel races decided atomically by the DB. Triggers self-cancel stale cards. Mute drops pending (not queued).
- No talking stick (roadmap). Free-flow discussion; mic mute is self-serve + therapist can force-mute; **Brio never touches mics** (may suggest).
- Participation model: `airtimeMs` (from LiveKit speaking detection — works without STT) + `actionCount` (reactions/selections; **pass = a selection answer, counts**). Equity: share of airtime vs `expectedParticipationWeight` (live-adjustable; **0 = never targeted**). Derived at read, never stored: share, deficit, promptsUsed (count fired intents per target), ladder level (from intent history).
- Draw-out ladder: L0 unnamed group cue (voiced) → L1 selection pushed to child's screen → L2 named invite always carrying a pass. Escalate only if prior level drew no action.
- Trigger defaults (all config; clinical calibration is week-1 roadmap): deficit >40% below weighted expectation · silent >3min · per-child cooldown 4min · prompt ceiling 3/session · idle 30s → re-engage · dominance >2× weighted share over rolling 5min → panel suggestion only.
- LLM: **no post-generation eval pass** (latency); zero-cost string sanity checks only; prototype **waits** for completions (no fallback race; failed call = no card, trigger re-fires). Therapist preview is the judge.
- Notes: ONE end-of-session LLM call writes ALL children's notes (counts first, quotes only child's own words, no clinical inference) → `postSessionNote` drafts. Session transcript = mechanical concat → `sessions.transcript`.
- Conventions: `participants._id` **is** the LiveKit identity · presence from LiveKit only (never stored) · `_creationTime` for inserts · sessions URL = unguessable id = the only auth in v0.
- UI flow (no login): create session (→ you're therapist, get link; form = exerciseDescription + defaults) → others open link, enter name → join as child → lobby (therapist sets preSessionNotes + weights) → start locks joins → active → end → **the same URL becomes a permanent review page** (timeline of utterances/actions/intents incl. do_nothing debug toggle, flags, notes editor).
- Terminology: **reaction** (always-available emoji/thumbs) & **selection** (Brio-pushed choice; answer may be "pass"). Not tap/chip.

## Stack & infra (locked)

- Web: React + Vite + TS + `@livekit/components-react` → **Render Static Site** (free).
- Brain: Convex (plain actions for LLM calls — NOT @convex-dev/agent) → **Convex Cloud** free tier.
- Worker: LiveKit agents-js (Node) → **LiveKit Cloud hosted agents** ($0 standing — billed only during sessions at $0.01/min, 1,000 min/mo free; `lk agent` CLI deploy, rollback, built-in logs/traces). Fallback ladder: Render Background Worker ~$7/mo → laptop during review window. Same code everywhere. Caution: use distinct agent names (or a dev project) so a local dev worker never double-registers against the deployment.
- Media: **LiveKit Cloud** Build tier (free: 5,000 WebRTC min, 1,000 agent min, Inference credits). One project for dev+prod; room name = sessionId.
- Models: **OpenAI only** — `gpt-5-mini` (actor/evaluator/notes; verify current small-model name at build time) + OpenAI TTS (e.g. `gpt-4o-mini-tts`, verify) . STT = LiveKit Inference (pick model string at build; OpenAI STT is the fallback swap).
- Secrets: Convex env = `LIVEKIT_API_KEY/SECRET` (room JWTs minted in a Convex action — never client-side) + `OPENAI_API_KEY`. Worker env = LiveKit keys + `OPENAI_API_KEY` + `CONVEX_URL`. **Two keys total.**
- Monorepo: `src/` = npm workspace root (keeps repo root clean). `src/backend` = `@briocare/backend`; web/worker will import `@briocare/backend/convex/_generated/api`. Add `"web"`, `"worker"` to `src/package.json` workspaces when created.

## Current state (done)

- [x] **Phases 1–3 COMPLETE & live-verified (overnight autonomous run, 2026-08-30 ~01:00–02:00)** — backend (all functions + 32 keyless tests green), media worker, web app (all flows browser-tested). Root `npm run dev` (concurrently: db+worker+web) & `npm test` wired; README rewritten with exact run/test commands (kept `context/` pointers). **Nothing committed — Milton reviews first.** Phase 4 (sims/evals/demo scripts) + Phase 5 (deploy/submit + doc-truth pass) remain.
- [x] Demo artifact from UI verification: session `js77hx3yh7a5dbhymtetv4qevx8dfrsa` — its review page (http://localhost:5173/s/js77hx3yh7a5dbhymtetv4qevx8dfrsa) shows the whole story: introduce → re_engage → reaction → L1 selection → pass answer → distress line → flag → vetoed card, with notes + transcript + audit toggle.
- [x] Research, PRD v0.8, TDD v0.6 (incl. deployment section + design-journey appendix) — committed by Milton.
- [x] PRD v0.9 — 7 illustrations placed + republished (same artifact URL). **TDD still has no figures**; the four diagrams deliberately left for hand-authored SVG are listed at the bottom of `docs/image-brief.md` (therapist panel, airtime arithmetic, trigger→actor→gate, phase timeline).
- [x] `src/` workspace + `src/backend/convex/schema.ts` — 6 tables (sessions, participants, utterances, actions, agentIntents, flags), guideline-compliant index names (`by_sessionId`, `by_sessionId_and_state`, …), validated & deployed on **Convex Cloud**: project `briocare`, team `milton-x-ren-gmail-com`, dev deployment **`zany-bee-795`** → dashboard https://dashboard.convex.dev/d/zany-bee-795 (Milton watches here while we develop). Reviewer path still = anonymous local (`npx convex dev` on a fresh clone, no account).
- [x] Convex AI files installed (`backend/CLAUDE.md` → read `convex/_generated/ai/guidelines.md` before writing Convex code; skills in `backend/.claude/skills/`).
- Schema notes: `agentIntentType` union is a **working set** (do_nothing, draw_out, re_engage, affirm, suggest_to_therapist, raise_flag, respond_to_cue, introduce, block, cut_off, link) — free to change while dev DB is empty. `raise_flag` rows are born `executed`.

## Implementation plan

Walk step by step with Milton; check off as we go. 🧑 = needs Milton.

### Phase 0 — accounts & spikes ✅ COMPLETE
- [x] 🧑 LiveKit Cloud project `briocare` + `lk cloud auth` on this device; keys in both env files (note: worker uses **`.env.local`**, not `.env`; key renamed `OPENAI_API_SECRET_KEY` → `OPENAI_API_KEY`)
- [x] Convex deployment env set via `npx convex env set`: OPENAI_API_KEY + LIVEKIT_URL/API_KEY/API_SECRET — set on cloud dev deployment `zany-bee-795` (env vars are per-deployment; re-set them if the deployment ever changes). Spike B re-verified against cloud.
- [x] 🧑 Convex plugin installed (convex-expert agent, MCP tools, `convex:design` skill available)
- [x] **Spike A ✅** (`worker/src/spike-agent.ts`, `spike-talker.ts`, `spike-stt-direct.ts` — throwaway/reference): auto-dispatch works; TTS voice track published and heard; per-track Inference STT gives speaker-attributed finals; ActiveSpeakersChanged gives per-identity speaking events.
- [x] **Spike B ✅** (`backend/convex/spikes.ts` — throwaway): Convex action → OpenAI structured output round-trip works.

**Phase-0 discoveries (carry into the real build):**
- `gpt-5-mini` confirmed (resolves `gpt-5-mini-2025-08-07`). **Must pass `reasoning_effort: "minimal"`** — default thinks for ~7.6s; minimal ≈ 2.0–2.4s. Speed fallbacks: gpt-5-nano / gpt-4o-mini.
- **Actor over-personalization observed live**: recommendation was L0 *unnamed group cue*; model wrote a *named* Maya invite anyway. System prompt must define ladder-level semantics hard (TDD §06 failure, empirically confirmed on day one).
- agents-js 1.7.1: `defineAgent({entry})` + `cli.runApp(new ServerOptions({agent: fileURLToPath(import.meta.url)}))`; no `agentName` → auto-dispatch to every new room; `tsx src/agent.ts dev` works ("dev mode deprecated → use `lk agent dev`"). Standalone use of agents libs requires `initializeLogger()`.
- **Inference STT does NOT resample** (default 16 kHz; gateway rejects 48 kHz). Fix: `new AudioStream(track, {sampleRate: 16000, numChannels: 1})` — rtc-node resamples natively. `deepgram/nova-3` works; finals carry word timings + confidence.
- **rtc-node native-boundary bug (the interview bug story)**: an `Int16Array` **subarray view** passed to `new AudioFrame(...)` → `captureFrame()` ships garbage (byteOffset ignored — correct energy + clock, zero words, no interims). Use `.slice()` (copy). Diagnosed via file→STT direct test (perfect) vs room path (empty), then slice-vs-subarray discriminator.
- OpenAI TTS: `new openai.TTS({model:"gpt-4o-mini-tts", voice:"nova"})` from `@livekit/agents-plugin-openai`; `synthesize(text)` → ChunkedStream `{frame}` → `AudioSource.captureFrame`; publish `LocalAudioTrack.createAudioTrack` + `TrackPublishOptions({source: TrackSource.SOURCE_MICROPHONE})`; AudioSource rate = `ttsEngine.sampleRate`. captureFrame paces in real time (blocking ≈ audio duration).
- Convex: typed env via `convex.config.ts` `defineApp({env}) ` + `import { env } from "./_generated/server"`; TypeScript installed in backend (tsc 7.x); a plugin hook typechecks convex/ after every write; guidelines file read (validators everywhere, internal* for private fns, fetch OK in default runtime, scheduler patterns, convex-test setup).
- Sim-persona seed: talker WAVs via macOS `say` + `afconvert -f WAVE -d LEI16@48000 -c 1`.
- **dotenv must use `override: true`** in worker scripts — Milton's shell exports a stale `OPENAI_API_KEY` (ends `TmoA`, 401s); without override the shell wins over `.env.local`.

### Phase 1 — backend functions (Convex; keyless-testable core) ✅ COMPLETE
- [x] Session lifecycle in `sessions.ts`: create/join/start/end + `resolve` (string-tolerant), `roster`, `stats` (derived, client passes `now`), `captions`, `review`. end = cancel pendings + transcript concat + schedule notes.
- [x] Therapist commands in `therapist.ts` (+ `requestMove` mutation → schedules actor with 1-item menu; therapist tap = approval → autoExecute runAfter(0)). `flags` query + `ackFlag` there too.
- [x] `interactions.ts`: recordAction (rate limit 6/10s — excess logged, not counted), `recentActions`, `mySelections` (executed draw_out L1 targeting me).
- [x] Worker-facing `worker.ts`: `view` (one subscription: status+muted+roster+pending+executed), recordUtterance (+tripwire; NO airtime write), bumpAirtime (sole airtime writer), `participantForToken`.
- [x] Intents in `intents.ts`: approve/cancel (public, guarded), autoExecute (guards state+active+muted+dial at fire time), cancelAllPending helper, `feed` (no do_nothing) / `audit` (everything).
- [x] Triggers pure in `lib/triggers.ts` + `lib/participation.ts` (evaluated in `engine.tick` internalMutation every 5s — NOT after each write; simpler, ≤5s latency).
- [x] Actor: `engine.actor` internalAction → `actorContext` internalQuery → one OpenAI call → `commitDecision` internalMutation (constraints re-checked ATOMICALLY vs live state; every path lands a row incl. vetoes as do_nothing w/ reason). Pre-session notes are in the actor prompt.
- [x] Evaluator every 2nd tick (~10s) → `commitFlag` (dedupe by utteranceId; also writes raise_flag intent row born executed).
- [x] `notes.ts`: generate on end (only fills empty notes) + public `regenerate` (overwrites).
- [x] **32 vitest tests, all green, keyless** (`src && npm test`): triggers/participation/constraints pure + convex-test integration (guarded races w/ fake timers + `finishInProgressScheduledFunctions`, dial flip mid-window, mute cancels, L0-name-ban veto, rate limit, tripwire, flag dedupe, stale sweep, keyless tick→actor→do_nothing audit trail).

### Phase 2 — media worker (`src/worker/`) ✅ COMPLETE
- [x] `src/agent.ts` (spike files kept as reference). Explicit dispatch verified: worker registers `ServerOptions({agentName:"brio"})`; `sessions.start` → `livekit.dispatchAgent` internalAction → `AgentDispatchClient.createDispatch(sessionId, "brio")`. Run: `npm run dev -w @briocare/worker`.
- [x] Per-participant Inference STT (16k AudioStream); STT finals → recordUtterance with burst-derived timestamps; `sttOk:false` fallback row when a ≥1.5s speaking burst gets no transcript within 2.5s.
- [x] Airtime: ActiveSpeakersChanged → per-identity speakingSince; 3s heartbeat + burst-end flush via bumpAirtime.
- [x] Playback: one ConvexClient `onUpdate(worker.view)` drives everything — pre-synthesize on pending, serial play-queue on executed, `agentMuted` checked per frame (mid-sentence mute works), executed-before-connect skipped (no history replay). NOT voiced: draw_out L1 (on-screen only) + suggest_to_therapist.
- [x] Brio tile: `localParticipant.updateName("Brio")` + metadata `{kind:"brio"}` — works (tile shows "Brio"). Roster from view = STT allowlist (unknown identities ignored).

### Phase 3 — web (`src/web/`) ✅ COMPLETE (all flows browser-verified)
- [x] Vite+React18+TS; `main.tsx` (ConvexProvider + router), routes `/` and `/s/:sessionId`; LiveKit token via `api.livekit.mintRoomToken`; graceful no-media fallback.
- [x] `pages/Home` (create), `pages/Session` (resolve → Join/Lobby/Active/Review by status+identity). Identity = **sessionStorage per tab** (multi-tab demo = multiple people) + localStorage persisted ONLY for the creator + explicit "Continue as X" resume card (no silent impersonation).
- [x] Kid view: tile grid (Brio tile incl.), captions strip, emoji reaction bar + float, selection overlay for L1 (fixed 3 options, pass built in — actor writes only the question), therapist force-mute honored client-side.
- [x] Therapist panel: flags banner (+ack) on top, Brio card (mute/dial/cue/named moves w/ target picker), intent cards (pending highlighted, ✓ Now / ✕ Veto, countdown bar in autonomous, canceled shows reason), equity ledger (share bar vs expected marker, weight slider, per-kid mute, promptsUsed, quiet-for).
- [x] Review page on same URL: merged timeline (utterances/actions/intents/flags; do_nothing behind "full audit" toggle), notes editor (+ Regenerate drafts), transcript in <details>.
- [x] Sage tokens lifted from PRD (light+dark), Newsreader/Public Sans/Plex Mono via Google Fonts. `npm run build` passes.

### Phase 4 — sims, evals, demo
- [ ] Sim personas (dominator, silent one, tangent-chaser, eager helper): scripts driving mutations directly (headless) AND joinable as room participants for live demo (stretch: TTS audio into room so they're audible).
- [ ] `npm run demo`: seeds a session with sims, prints the therapist link.
- [ ] Eval harness `npm run eval`: scenario scripts → metrics (participation Gini, silent-persona time-to-first-action <4min on / never off, ceiling respected, **unnecessary-action rate** vs annotated do_nothing scenarios) + **invariants** (suggest-only never auto-executes; nothing while muted; spoken lines trace to rows born with that text; canceled never reach audio; distress → flag + zero voiced; weight-0 → zero targeted intents; every decision has prompt+response) → writes markdown report. Scripted-actor mode = keyless; live-actor mode needs OPENAI key.
- [ ] Demo script doc: create → sims + Milton-as-kid join → cue intro → exercise → visible draw-out ladder → sim distress line → flag → end → review + notes. Latency numbers measured & noted for TDD.

### Phase 5 — deploy & submit
- [ ] 🧑 `npx convex login` in `src/backend` → link project, `npx convex deploy`; set prod env vars.
- [ ] 🧑 Render: Static Site (root `src/web`, build `npm run build`). Worker: deploy to **LiveKit Cloud agents** (`lk agent create/deploy`, secrets via `lk agent` CLI); fall back to Render Background Worker or local if the cloud build eats time.
- [ ] Root `README.md`: exact run command (target: `cd src && npm install && npm run dev` doing convex+web+worker via concurrently), exact test command, env setup, reviewer access notes (scoped keys), known limitations (no auth; sessions=unguessable URLs; adult/synthetic data only; name pronunciation; child-ASR unvalidated; single exercise).
- [ ] Final doc pass: update TDD if implementation drifted (it must match reality — reviewers compare); republish artifacts (same URLs); Milton shares artifact links or exports.
- [ ] 🧑 Submission email per format above; then the AI interview within ~30min (repo open; appendix in TDD = evolution story cheat-sheet).

## Open decisions — ALL RESOLVED during the overnight build
- Model strings: `gpt-5-mini` (reasoning_effort minimal) / `gpt-4o-mini-tts` voice nova / `deepgram/nova-3` — in `lib/constants.ts` MODELS.
- pending→executed: the mutation sets executed (approve or scheduled autoExecute); worker plays on seeing executed. ✓ built.
- Captions: finals-only via Convex. ✓
- Force-mute: client-honored flag (README known limitation). ✓
- Suggest-only cards: no timer; stale-cancel at 45s or when the target speaks. ✓

## "Brio leads" iteration (with Milton, morning after — all live-verified)

The demo models a BREAKOUT ROOM: kids + Brio in the room, therapist supervising from outside. Changes:
- **Three-dial autonomy** (schema union changed): `autonomous` = instant execute (DEFAULT; mute is the brake) · `auto-with-delay` = the old behavior, veto window now 3s · `suggest-only` unchanged. autoExecute guard blocks only suggest-only; commitDecision schedules per dial. Panel = 3-way segmented control; countdown bar shows only in auto-with-delay.
- **The handoff gate**: engine proposes NOTHING until an `introduce` intent executes (derived from intents — no schema field). Therapist moves bypass. UI: big green **"👋 Hand to Brio"** button ON Brio's video tile (therapist only, custom GridLayout tile via useTrackRefContext; Brio tile = identity not in roster), disappears once introduce exists; backend rejects a second introduce. The handoff cue = introduce AND open the exercise in one utterance.
- **The round**: post-handoff facilitation trigger — kids "not heard since handoff" (lastUtteranceAt ≤ introducedAt) + floor free → wake with menu [draw_out, affirm, link, re_engage, do_nothing], **no pinned target: the ACTOR picks from the full timeline** (who-shared is transcript semantics, not arithmetic — Milton's call). Round invites = ladderLevel 2 (named + pass = the norm in a facilitated round); the gentle L0→L1→L2 ladder remains for equity triggers. ROUND_COOLDOWN 25s/child; ceiling still applies.
- **Merge-at-write** (the fragmented-transcript fix): recordUtterance extends the same speaker's previous row when the new final starts ≤ THOUGHT_GAP_MS (2.5s) after it ended — one row per THOUGHT. Fixes actor context, transcript, review, first-share detection, and the tripwire now catches phrases split across fragments (re-runs on merged text; utteranceId-deduped). Interjections by others don't break a speaker's merge (per-speaker last row).
- **Human pace chain** (all 2.5s = THOUGHT_GAP_MS): recordUtterance schedules `engine.wakeCheck` at +2.5s (bails if newer speech — only the check after the true thought-end evaluates); triggers require floor-free ≥ ~2.1s; the WORKER waits for 2.5s of live room-silence before playback (cap 8s) — it alone has the live speaking signal. Tick (5s) remains idle-detector/fallback. Net: voice ~4-6s after a kid truly finishes.
- **BRIO_GRACE_MS 15s**: after Brio's own voiced line, round/equity/idle hold (the floor belongs to the kids); first_share affirm + dominance exempt. Fixed the observed pile-up (invite 3s after intro; second invite before the first could be answered).
- **Commit-time dedupe**: two wakes can evaluate while one LLM call is in flight (debounced check + tick ~2s apart) — commitDecision re-checks a 10s engine gap where mutations serialize; the runner-up lands as audited do_nothing "superseded". Observed live before the fix (double affirm/invite), single-voice after.
- **Actor context uncapped** (Milton: no cap for demo): FULL session timeline — merged utterances + reactions + selection answers (passes!) + Brio's own executed moves, chronological with mm:ss stamps (bounded only by DB take(600)/take(300) reads). Prompt persona: "you run the exercise; the therapist supervises from outside; read the timeline for who truly shared/passed" + anti-repeat rule (observed working: after an ignored invite it re-engaged the group with fresh phrasing instead of repeating).
- **Supervise from outside**: therapist toggle — leaves the LiveKit room entirely (panel + captions only), "Join the room" returns. Per-tab (sessionStorage). Kids then see only each other + Brio.
- ngrok: Milton added `allowedHosts: [".ngrok-free.app"]` to web/vite.config.ts (any tunnel subdomain works).
- **"Only re-engages + long silences" (Milton's real-human test)**: once every child had shared, `notYetHeard` emptied → the ONLY trigger left in silence was idle with menu [re_engage, do_nothing] at 15s idle + 45s cooldown → 20–50s dead gaps and re-engage forever. Fix: **followup wake** (reasonCode "followup") when everyone's been heard + floor quiet > FOLLOWUP_IDLE_MS (12s) + ≥ FOLLOWUP_COOLDOWN_MS (25s) since Brio's last voice AND last engine decision. Full menu [draw_out, link, affirm, re_engage, suggest_to_therapist, do_nothing]; draw_out L2 doubles as a CONTENT FOLLOW-UP ("Maya, any favorite dog you saw at the park?" — observed live). **Unresponsiveness guard**: a child whose last invite went unanswered (lastTargetedAt > their lastUtteranceAt) can't be re-targeted; if ALL recently-invited kids are unresponsive, menu drops to [re_engage, suggest_to_therapist, do_nothing]. Idle branch now fires only while someone is still unheard (followup owns quiet moments after). Observed arc in a dead room: affirm → fresh-angle re_engage ("if your favorite thing had a sound…") → suggest_to_therapist ("room has been quiet… check in briefly") every ~30s — de-escalates to the human instead of looping. Known nit: actor may affirm the same kid twice before varying. 48 tests → 46 (net, incl. followup + guard).
- **The Kay incident (real-human test — the worst failure so far)**: Brio invited one kid FIVE times, twice AFTER she said "I want to pass". Three missing guards, all now deterministic: (1) **verbal-pass detection** — `looksLikePass()` (short utterance ≤40 chars containing "pass"; selection answer "Pass this time" counts too) → PASS_COOLOFF_MS 3min during which the child is excluded from EVERY targeting trigger (first_share, round, followup, equity) AND hard-vetoed at commit (`constraint veto: … passed recently`) — prompt rules alone were ignored by the model (observed: it re-targeted Leo 2s after his pass; the DB veto stopped it). Pass also FINAL in the system prompt now. (2) **draw-out ceiling enforced at commit** — followup had bypassed the trigger-side ceiling; now commitDecision counts executed engine draw_outs per target (index read) and vetoes at PROMPT_CEILING regardless of trigger path. (3) **FOLLOWUP_TARGET_COOLDOWN_MS 60s** — no consecutive follow-ups at the same kid. All-excluded → group-only menu wake. Therapist moves exempt from all three (their call). passNote names passed kids inside recommendations. 51 tests.
- **Soft-decline iteration (next real-user test)**: run was structurally good (round, content follow-up "what makes AI cool for you?", guards prevented pestering, varied recovery) but exposed: kids don't say "pass" — Millie: "No. I'm good.", Boba: "I'm past" (STT garble of pass); Brio then AFFIRMED Millie's decline ("Thanks for sharing"), and Boba's real first share was never affirmed (first_share's ROUND_COOLDOWN check blocked affirming a kid who just ANSWERED our invite). Fixes: (1) `looksLikePass` now includes "past"; (2) `looksLikeSoftDecline` ("no/nah/nope…", "I'm good/okay/fine", "no thanks", "not right now") counts as a pass ONLY within DECLINE_REPLY_WINDOW_MS (30s) of an executed intent targeting that child — conjunction keeps precision ("I'm good" as content ≠ decline); enforced trigger-side AND in the commit veto. (3) first_share condition is now "spoke since being targeted (or never targeted)" — answering an invite is the natural affirm moment. (4) Prompt: soft declines are passes; NEVER affirm a decline (do_nothing instead). 54 tests.
- **Yielded-affirm vanishing (Ana never acknowledged)**: root cause was NOT model disobedience — the menus made compliance impossible: (1) the yielded affirm counted as "targeting" Ana → 60s target-cooldown excluded her → all-unresponsive → group-only menu with no affirm; (2) the hard constraints name-banned re_engage, so even a bridge ("Loved that, Ana! Okay friends…") would have been vetoed. Fixes: **lastTargetedAt now counts only executed intents + therapist vetoes** (yielded/stale/muted attempts never reached the child and don't lock them); **re_engage removed from the name-ban** (L0-invite ban stays — the Spike-B case) with prompt guidance: may OPEN with a named acknowledgment, the ask stays group-addressed; prompt fold-in rule hardened ("if an affirm was yielded, that child was never acknowledged — your next line MUST open with it"). 58 tests (incl. yielded-doesn't-cooldown, named-re_engage-legal).
- **"Totally fine to pass" chanting**: the model was parroting the prompt's own example strings (7×/session). Fix: examples rewritten with two DIFFERENT outs; rule = intro sets the norm once, then rotate light touches ("no pressure" / "only if you feel like it" / "or skip, that's fine"), never the same out twice in a row, and NO out-phrase for kids who already shared/passed (first-time invitees always get a clear out). Verified live: consecutive invites used "No pressure!" then "Or skip, that's fine." Same transcript also showed duplicate panel notes 15s apart → suggest_to_therapist joined the menu-guard pattern: no second suggestion within 90s (`lastSuggestionAt` input, same trim as re_engage); if a trimmed menu collapses to [do_nothing] the wake is skipped entirely (saves the LLM call). 57 tests.
- **OPT-BACK-IN + ONE-PROMPT-IS-ENOUGH (the four-re-engages transcript)**: two one-condition fixes. (A) A pass holds only while it is the child's LATEST utterance — a later voluntary (non-decline) share clears it (reactions don't: a 👍 isn't "I want the floor"); applied in both the trigger-side passedAt map and the commit-time veto. (B) `lastReEngageUnanswered` (last voiced engine move was re_engage && no child speech since): drops re_engage off every followup menu and silences the idle branch until someone answers — deterministic escalation curve: one fresh angle → silence → suggest_to_therapist → quiet. Live-verified in one arc: Leo passed → fast group response → Leo opted back in with bunnies → REAL actor invited "tell us one thing the bunnies did?" 3s later (pass cleared) → dead air → one lightning-round → next decision was a therapist note, not another activity. 57 tests.
- **GRACE-UNTIL-RESPONSE (Milton's simplification — killed the pass-response-wake proposal)**: instead of a dedicated wake for the decline edge case, ONE rule: Brio's post-speech grace ends EARLY the moment any child responds (utterance/reaction/pass — a response IS the floor handing back); only true silence runs the 12s clock. DELETED: FOLLOWUP_IDLE_MS + FOLLOWUP_COOLDOWN_MS — the follow-up phase now obeys the same three pacing concepts as everything else: (1) 2.5s thought-gap debounce, (2) MIN_GAP 12s + grace-until-response, (3) the delivery gate. Consequence: post-handoff, followup outranks the equity ladder whenever everyone's been heard (L0/L1 ladder is now the fallback path for edge states, not the main leading flow — TDD doc-pass note). Bridge behavior is PROMPT-ONLY: "bridge in one breath — half-sentence thanks to the unacknowledged sharer, then your move; accept a pass warmly in the same line, never dwell; a yielded acknowledgment folds into your next line's opening" (no engine hint yet — add the deterministic nudge only if the model proves sloppy). Live-verified: Leo's "I want to pass" → next decision in 4s (was 26s), fresh group move, no re-invite. 56 tests.
- **THE DELIVERY GATE (Milton's design call — replaced the text-heuristic patches)**: "the brain decides, the floor decides delivery." Lifecycle grew one honest stage: pending → executed → **voiced** (worker stamps `voicedAt` when playback finishes) or → canceled(**yielded**) (floor never opened). Worker-side gate before playing an executed line: wait for no-human-speaking + 2.5s silence (live ActiveSpeakersChanged); floor busy > YIELD_BUDGET (12s) → `api.intents.yieldIntent` gives the line back, audited. **Barge-in**: human talking >800ms while Brio speaks → Brio stops mid-line (always yields to children). Engine: BRIO_GRACE now keyed to real voicedAt; "one card in flight" extended — an executed-but-unvoiced line blocks new wakes (30s bound so orphans can't wedge the engine). DELETED wholesale: looksUnfinished + UNFINISHED_GAP + all threading (triggers input, wakeCheck branch, politeness text-tracking, prompt rule) — merge-at-write stays at plain 2.5s (the Charli fix). **Actor context now carries the WHOLE decision history**: every intent incl. do_nothing (with reason) and canceled attempts rendered as "(you tried X → name: '…' — CANCELED (yielded); the children never heard it)" + prompt rule: unsaid lines are unsaid, yielded moves may be retried if the moment returns. Live-verified: cue fired into a continuously-talking room → held 12s → yielded (5 yields total, 0 barge-ins); after the room quieted, next line waited the 2.5s gap and voiced with stamp. Schema delta: voicedAt + "yielded" reason. Two new guarded mutations (markVoiced, yieldIntent) in the approve/cancel pattern. 56 tests. Interview line: "the engine picks moments from data; the worker owns the microphone and never talks over a child; unspoken lines are given back and audited."
- **The slow-talker test (Boba: "testing that I should not be interrupted" — Brio interrupted)**: kids pausing >2.5s mid-thought beat every gap-based threshold, AND his fragments visibly trailed off ("…And", "…A") — a deterministic unfinished signal we ignored. Fix: `looksUnfinished()` (trailing conjunction/article/preposition/comma from a dangling-endings list) stretches every gap decision from THOUGHT_GAP (2.5s) to UNFINISHED_GAP_MS (8s): row-merge window in recordUtterance, wakeCheck bail, trigger floor-free check, and the worker's politeness wait (cap raised to 15s; worker imports the helper from backend lib — one definition). Plus prompt rule: trailing-off last line → do_nothing. Also confirmed working this run: Millie's "No. I'm good." decline → no affirm, no re-invite, clean group-move recovery. 55 tests.
- **Cross-talk known limitation (from same test)**: one person's mic picks up another's speech → duplicate garbled rows attributed to the wrong speaker (Rivera's row = Millie's words). No diarization across mics in v0 — demo with headphones or separate rooms/devices. Add to README limitations at Phase 5.
- **Missed-track bug (real-human sessions only)**: humans are already in the room when Brio's job starts, so their TrackSubscribed events fire during ctx.connect — BEFORE the handler attaches (~220 lines of awaited setup later) → zero STT ("only Brio's lines appear"). Never seen in talker rehearsals because the talker always joined after the worker. Fix in agent.ts: sid-deduped `transcribeTrack` + a sweep of `room.remoteParticipants[].trackPublications[].track` right after attaching the handler; roster gate moved from stream-start to per-final (roster can also still be loading). Rule of thumb: with LiveKit agents, ALWAYS handle already-present participants, not just events.
- Tests now **43 keyless** (gate, round + actor-picks-target, grace, per-dial lifecycle, merge/split/interjection, split-phrase tripwire, wakeCheck debounce, dupe-introduce rejection).
- TDD drift adds (Phase-5 pass): three-dial semantics + instant default, handoff gate + tile button, round wake + actor-chosen targets + L2 round norm, merge-at-write, THOUGHT_GAP/BRIO_GRACE/commit-dedupe, supervise mode, VETO 3s, SILENT 90s, MIN_GAP 12s, IDLE 15s.
- **Frontend polish round (post-v0.8, Milton's 4 asks — all live-verified in the pane)**: (1) Home prefills "Dr. River" → one-click session. (2) Tile mute: therapist sees a Mute/🔇 Unmute pill top-right on each KID tile (calls setParticipantMuted; panel button unchanged); KidView force-mute effect made TRANSITION-aware so unmute re-enables the mic (mount with muted=false never force-enables a kid's own choice). (3) The bottom captions strip is GONE from in-room views — replaced by per-tile captions (.tilecap): each avatar shows that person's latest merged line; Brio's tile shows its latest voicedAt line (yielded/vetoed never show) from intents.feed sorted by voicedAt; Brio's tile turns GREEN (`brio-live`: painted placeholder + ring, driven by hasIntroduced) once handed off — kids see all of this too. (4) Supervise mode (and the no-media fallback) got `TranscriptLog` (renamed from Captions, only ActiveRoom used it): full scrollable log, kid utterances + Brio's VOICED lines merged by time, mm:ss via fmtClock from startedAt, auto-scroll via container scrollTop (not scrollIntoView — that scrolls ancestors). sessions.captions take 30→60 (serves tiles + log). Verified live end-to-end: handoff → green tile → intro/invite voiced captions, seeded utterance (worker:recordUtterance is PUBLIC — `npx convex run` works for demo seeding) → tile caption + Brio affirm, mute round-trip incl. kid banner, supervise log interleaved correctly.
- **Mute state sync (Milton: "mute should sync… state stored both in convex and livekit")**: `participants.muted` is now the ONE readable truth = the kid's ACTUAL mic. Kid client (KidView) runs two effects: Convex→LiveKit `setMicrophoneEnabled(!me.muted)` on every muted change (therapist's command, both directions), and LiveKit→Convex via ParticipantEvent listeners (TrackMuted/TrackUnmuted/LocalTrackPublished/LocalTrackUnpublished on localParticipant, mic-source filtered) that write setParticipantMuted only when Convex and the mic DISAGREE (`mutedRef.current === enabled`) — self-caused events see them already equal, so no feedback loop; both directions converge. The "🔇 Your therapist muted your mic" banner DELETED (kid's ControlBar mic button is the interface). Backend reads nothing from participant.muted (grep-verified) so the semantic shift is free; enforcement still client-side (README limitation stands — kid CAN override, and the dashboard then truthfully shows unmuted). Verified all 4 arcs in the pane with a **getUserMedia polyfill** (silent AudioContext dest + canvas captureStream injected pre-join — real tracks publish, real mute events fire; great pane-testing trick): kid self-mute→tile flips red Unmute; therapist unmute→kid's real mic re-enables; therapist mute→mic off, no banner; kid override→tile truthfully back to Mute. Zero console errors, 58 tests.
- **Tile polish round 2 (Milton's 3 asks)**: (1) Tile captions now EXPIRE — lastLine/brioLine carry `{text, at}` (utterance endAt / intent voicedAt), Grid ticks `useNow(2000)`, Tile renders only when `now - at < CAPTION_TTL_MS` (12s, in ActiveRoom) + a 300ms fade-in keyframe. (2) Name+mic pill redesigned via CSS on LiveKit's own metadata DOM (`.lk-participant-metadata-item`): dark blurred 999px pill, 14px/600 white name, and `:has([data-lk-muted="true"])` turns the WHOLE pill red when muted (attribute verified in live DOM); Brio's pill tinted green when live. (3) **The Brio character**: docs/img/2.webp is a 3-pose sprite sheet → PIL-cropped squares at src/web/src/assets/brio-{calm,talk}.png (crops (80,110,640,670)/(625,110,1185,670)); when `brio-live`, default placeholder svg hidden and the character renders as a centered circular badge (`.brio-face`, 58% height, radius 50%) — **pose swaps to brio-talk + scale(1.06) while `useIsSpeaking(trackRef.participant)` is true** (LiveKit audio-activity on the worker's TTS track — verified live catching talk-pose mid-intro, calm after). Screenshot-verified the whole board: red/dark/green pills, character badge, captions gone when stale. tsconfig already had vite/client types so png imports just work.
- **Marin voice + dead circle**: Brio's TTS voice → **"marin"** (gpt-realtime's warm voice). Milton "couldn't find it" because the LiveKit openai plugin's `TTSVoices` union is STALE (alloy…shimmer only) — but the plain `/v1/audio/speech` endpoint with gpt-4o-mini-tts ACCEPTS marin (verified live, HTTP 200, sample sent to Milton). Voice now lives ONLY in `MODELS.ttsVoice` (constants.ts); agent.ts imports MODELS and casts via `type TTSVoice = NonNullable<NonNullable<ConstructorParameters<typeof openai.TTS>[0]>["voice"]>` (deep dist type import is blocked by the plugin's exports map). ⚠️ worker dev script is `tsx src/agent.ts dev` — NO watch: Milton must RESTART `npm run dev` to hear marin. Worker env lives in `src/worker/.env.local` (dotenv override — shell's stale OPENAI_API_KEY loses). The useless bottom-right hover circle on tiles = LiveKit's `.lk-connection-quality` indicator (non-interactive) → `display:none`, verified on all 3 tiles.
- **STT A/B (Milton: "is there better STT we can swap to easily?")**: built `src/worker/stt-ab.mts` (usage in its header) — pushes the same 16k PCM through `inference.STT` exactly like the worker, realtime-paced, prints finals+timing. Test audio = gpt-4o-mini-tts kid-voiced clips (normal excited + slow-halting with long pauses) hitting the observed failure cases. RESULTS: **nova-3** near-perfect but fragments slow speech (12 finals — our merge absorbs it) and hears "Rio" for "Brio"; **deepgram/flux** = model-level turn detection, held the whole halting thought through 3–4s pauses → ONE clean final (the deleted looksUnfinished problem solved at the model layer) BUT not a string-swap: our sttOk:false fallback (2.5s grace per burst) would spam "(inaudible)" rows and burst-based startAt mis-times multi-burst turns — flagged as post-demo upgrade; **assemblyai/universal-streaming** emits DUPLICATE finals via the gateway (formatted + raw turn concat) → would double-record, disqualified; **cartesia/ink-2** word-perfect but batches everything to end-of-audio (dead captions); **speechmatics/linden-1** dropped words; universal-3.5-pro in docs but gateway says "model not found". EVERY model garbled the invented name → provider-independent → **kept nova-3 + `modelOptions: { keyterm: ["Brio"] }`** (typed passthrough, verified: "Rio,"→"Brio," on identical audio). agent.ts now reads MODELS.stt (hardcode killed). Stack restarted (task bqkeqx074) — keyterm live. Gateway catalog reference: docs.livekit.io/agents/models/stt.
- **Milton retuned ENGINE_MIN_GAP_MS 12000→3000 himself** after the latency audit — "it got better". The 10s commit dedupe + grace-until-response now carry the spacing jobs MIN_GAP over-carried.
- **docs/manual.html — THE FIELD MANUAL (interview-prep tutorial, Milton's ask)**: standalone page, same sage design system + head as tdd.html (inline style block, Google Fonts links; NOT artifact-wrapped — no dot-file, opened locally). 14 sections: doctrines · system map · repo-in-reading-order (with line counts + what-to-say) · life-of-one-invite timeline · ERD + intent lifecycle · trigger stack (gates→priority SVG) · actor (prompt-vs-mechanism table) · veto stack/commit gauntlet · worker pump state machine · web room · safety · constants tables (CURRENT values incl. MIN_GAP 3000) · 12 war-story cards · 12-question interview drill (details/summary self-test). Six inline SVGs in the TDD idiom; verified programmatically (all sections/SVG heights sane, zero text overflow past viewBox, zero pairwise text collisions) + top-of-page screenshot. Footer warns: retuning constants ⇒ number-pass manual + TDD. Gotcha: the pane's local-file preview loads via data: URL in a wrapper — in-page JS scrolls don't move what screenshots capture, and a hidden pane times out mouse input; verify local files by DOM geometry instead.
- **Voice-latency audit (research only, no fixes — Milton asked why autonomous "takes a while")**: fast path ≈ 5–9s (STT final 0.3–1s → THOUGHT_GAP debounce 2.5s → actor 1–2s → commit+autoExec+sub ~0.5s → **full-line TTS 1–3s** → floor gate ~0 in a quiet room). Findings, ranked: (1) **autonomous skips the TTS pre-warm** — agent.ts pre-synthesizes only intents seen in `v.pending` (worker view), but autonomous executes via runAfter(0) so the sub delivers them already-executed → synth sits on the critical path; auto-with-delay DOES pre-warm during its 3s window, so its effective penalty vs autonomous is only ~0–2s. (2) **ENGINE_MIN_GAP 12s is creation-anchored and response-blind, and counts do_nothing** (engine.ts lastEngineIntentAt = newest engine intent of ANY state/type) — a kid answering Brio fast still waits out MIN_GAP (grace-until-response lifts only GRACE), and an invisible do_nothing mutes the engine 12s with nothing on the panel; (3) **blocked wakes have no scheduled retry** — a MIN_GAP/grace-blocked wakeCheck just returns; the next look is the 5s tick → +0–5s jitter on every held moment (silence-born followup/idle moves are tick-quantized too); (4) the yield path (Milton's remembered optimization) delays only in BUSY rooms: gate holds ≤12s then yields and the moment must be re-decided (20s+ or never) — by design, not a quiet-room delay; FLOOR_GAP 2.5s is already elapsed by decision time in quiet rooms. Worked example of the felt stall: Brio line created T-2/voiced T, kid replies T+3 (thought ends T+5) → wakeCheck T+7.5 blocked (MIN_GAP until T+10) → tick lands T+10–15 → +actor+synth → voice ≈ **8–15s after the kid finished**. Fix levers if ever asked: pre-warm executed-unvoiced too; anchor MIN_GAP to voicedAt or make it response-aware like grace; schedule a retry at gate expiry.

## Overnight run decisions (made autonomously — flag anything to revisit)
- **Schema change**: `agentIntents.ladderLevel` (optional number) added — L1 selections needed an explicit marker (worker must NOT voice L1; kid UI must render it). ERD in TDD needs this in the Phase-5 doc pass.
- Selection UI uses **fixed choices** ("Yes — I'll share!" / "Can I think a bit?" / "Pass this time"); the actor writes only the on-screen question. Keeps schema frozen + predictable kid UX.
- Triggers are tick-only (5s) — not evaluated inline after each write. Extra rules: ENGINE_MIN_GAP 20s between wakes (do_nothing counts), only ONE pending engine card at a time, IDLE_COOLDOWN 60s, dominance needs >45s window airtime (NB: with 2 equal-weight kids dominance mathematically can't fire — needs 3+), first_share affirm trigger (utteranceCount==1, <12s fresh).
- Failed actor call / missing key → **do_nothing row with the error as reason** (audit + natural 20s backoff). Evaluator do_nothing sweeps are NOT rows (10s spam); only raise_flag lands (flag + intent row).
- Therapist-source intents: born pending, autoExecute runAfter(0) — one uniform code path, dial-independent (their tap was the approval).
- `sessions.stats`/queries never read the clock — clients pass `now` (Convex guideline).
- Notes auto-gen fills only EMPTY postSessionNote; the review-page button overwrites.

## Phase 1–3 discoveries (beyond Phase 0's)
- `env` from `_generated/server` is literally `process.env` → vitest inherits Milton's stale shell OPENAI_API_KEY → `test.setup.ts` scrubs all keys (keyless tests stay keyless).
- livekit-server-sdk needs `"use node"` (node:crypto) AND lazy `await import(...)` inside handlers — a top-level import makes the module unloadable in the edge-runtime test VM (convex-test's import.meta.glob loads every convex/ file).
- A missing return-type annotation on a Convex action (livekit.mintRoomToken) collapsed the ENTIRE generated `api` type to `any` from the web project → every useQuery went untyped. Annotate action handler returns.
- ConvexClient (convex/browser) subscriptions work in the agents-js job child process (Node ≥22 global WebSocket; Node 25.6 here). One `onUpdate(worker.view)` drives the whole worker.
- **LiveKit closes the room when the last human leaves → the worker's job dies** (brain keeps ticking harmlessly). Real sessions keep the therapist tab in-room; empty-room sessions just have no media.
- Live latencies on cloud: actor decision 1.9–2.3s; notes 2.2s; dispatch instant. Full loop (speech→STT→trigger→actor→veto window→TTS in room) observed working repeatedly.
- Live-verified behaviors: affirm on first share (named), L0 group cues (correctly unnamed ×3), L1 on-screen selections, dominance→suggest_to_therapist (panel-only, not voiced), therapist cue introduce (on-brief), watchlist tripwire→flag→ack, suggest-only pending card→veto, review page with full audit trail, honest notes ("Spoke 0 times…" / quotes only own words).
- Browser-pane testing: read_page flaky on background tabs — use screenshots + javascript_tool; getUserMedia blocked (tiles = placeholders; audio can't be heard there — worker logs are the proof of voice).

## Phase-5 doc-truth checklist (TDD/PRD drift to fix before submission)
- ERD: + `agentIntents.ladderLevel`; selection = fixed 3 options; suggest_to_therapist born executed.
- Trigger table: tick 5s / evaluator 10s / MIN_GAP 20s / one-pending-card rule / stale 45s or target-spoke / dominance 45s-window guard / affirm first-share trigger.
- Evaluator do_nothing not logged as rows (only raise_flag).
- Identity model: sessionStorage per tab + creator-persist + "Continue as" (no silent impersonation).
- Latency numbers above; sttOk:false grace (1.5s burst / 2.5s wait).

## Verify-before-trusting (post-compression cautions)
- Re-check current API shapes at build time: agents-js multi-track STT + TTS publishing, LiveKit Inference model ids, OpenAI model names, Convex scheduler patterns. Use `backend/convex/_generated/ai/guidelines.md` for all Convex code.
- Budget: everything free except Render worker $7/mo (if used); OpenAI usage on Milton's credits; LiveKit free tier limits above.
